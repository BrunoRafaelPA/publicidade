import { Routes } from '@angular/router';
import { PublicidadeTableComponent } from './components/publicidade-table/publicidade-table.component';

export const routes: Routes = [
  { path: '', component: PublicidadeTableComponent },
];